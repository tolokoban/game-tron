div.tfw-view-combo { margin-left: 80px; display: inline-block; }
div.tfw-view-combo > header {
  position: static;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: start;
  font-size: 80%;
  opacity: .5;
  height: 16px;
  padding: 0 0 0 8px;
  margin: 0;
}
div.tfw-view-combo > button  {
  background: #fff;
  box-shadow: 0 2px 4px rgba(0,0,0,.5);
  height: 32px;
  border: 0;
  margin: 0;
  padding: 0;
  font-size: inherit;
}
div.tfw-view-combo > button.hide {
  overflow: hidden;
}
div.tfw-view-combo > button.hide > div.list {
  overflow: hidden;
}
div.tfw-view-combo > button.show {
  overflow: show;
}
div.tfw-view-combo > button.show > div.list {
  position: fixed;
}
div.tfw-view-combo > button.show > div.screen {
  position: fixed; left: 0; top: 0; width: 100%; height: 100%; background: rgba(0,0,0,.5);
}

div.tfw-view-combo > button > div.list {
  background: #fff;
  box-shadow: 0 4px 8px rgba(0,0,0,.5);
  transition: transform .2s;
  transform: translateY(0);
  overflow: auto;
  max-height: calc(100vh - 64px);
}
div.tfw-view-combo > button > div.list > div {
  position: static;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: start;
  height: 32px;
  max-height: 32px;
  min-height: 32px;
  padding: 0 8px;
  cursor: pointer;
  box-shadow: 0 1px rgba(0,0,0,.1);
}